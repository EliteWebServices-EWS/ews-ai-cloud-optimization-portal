# Sprint 10.5.6 – First AWS Backend Deployment

**Project:** EWS AI Cloud Optimization Platform (SISU'M)

**Sprint:** 10.5.6

**Deployment Date:** July 14, 2026

**Status:** ✅ Completed Successfully

---

# Objective

Deploy the SISU'M Express backend to AWS for the first time using AWS SAM and verify that the application is running successfully behind Amazon API Gateway.

This sprint marks the transition from a locally tested backend to a live AWS-hosted backend.

---

# Deployment Summary

| Item                  | Value             |
| --------------------- | ----------------- |
| Stack Name            | `sisum-backend`   |
| AWS Region            | `us-east-1`       |
| Environment           | `production`      |
| Provider Mode         | `mock`            |
| Deployment Method     | AWS SAM           |
| Deployment Status     | ✅ Successful      |
| CloudFormation Status | `CREATE_COMPLETE` |

---

# AWS Resources Created

The deployment successfully provisioned the following AWS resources:

* AWS Lambda Function
* Amazon API Gateway (HTTP API)
* Amazon CloudWatch Log Group
* Lambda Permissions
* CloudFormation Stack

---

# Lambda Configuration

| Property       | Value                      |
| -------------- | -------------------------- |
| Function Name  | `sisum-backend-production` |
| Runtime        | `nodejs24.x`               |
| Architecture   | `arm64`                    |
| Handler        | `dist/lambda.handler`      |
| Memory         | `512 MB`                   |
| Timeout        | `30 seconds`               |
| Execution Role | `SisumLambdaExecutionRole` |

---

# Environment Variables

The deployed Lambda function uses the following environment variables:

| Variable      | Value                                    |
| ------------- | ---------------------------------------- |
| NODE_ENV      | `production`                             |
| PROVIDER_MODE | `mock`                                   |
| LOG_LEVEL     | `info`                                   |
| NODE_OPTIONS  | `--enable-source-maps`                   |
| CORS_ORIGIN   | `https://elitewebservices-ews.github.io` |

---

# API Gateway

## API Type

HTTP API

## API ID

```text
zqe6cl0m15
```

## Base URL

```text
https://zqe6cl0m15.execute-api.us-east-1.amazonaws.com
```

---

# Smoke Tests

## Health Endpoint

### Request

```bash
curl -i https://zqe6cl0m15.execute-api.us-east-1.amazonaws.com/api/v1/health
```

### Result

* HTTP Status: **200 OK**
* Express application loaded successfully.
* Lambda invocation completed successfully.
* API Gateway routing verified.

---

## Workflow Endpoint

### Request

```bash
curl -i -X POST \
https://zqe6cl0m15.execute-api.us-east-1.amazonaws.com/api/v1/workflows/run \
-H "Content-Type: application/json" \
-d '{}'
```

### Result

* HTTP Status: **201 Created**
* Workflow execution completed successfully.
* Response returned valid JSON.
* Mock Provider executed correctly.

---

# Workflow Execution

The workflow successfully completed all orchestration stages.

Completed stages:

* Evidence
* Governance
* Financial
* Confidence
* Recommendation
* Execution
* Verification
* Learning

No workflow failures were detected.

---

# Recommendation Output

The workflow produced the following recommendation:

| Property              | Value                                           |
| --------------------- | ----------------------------------------------- |
| Recommendation Status | `DEFERRED`                                      |
| Recommendation        | Resize EC2 from `t3.large` to `t3.medium`       |
| Reason                | Production environment requires manual approval |
| Monthly Savings       | **$30.37**                                      |
| Annual Savings        | **$364.44**                                     |

---

# CloudWatch Verification

CloudWatch logging was successfully verified.

Observed execution metrics:

| Metric              | Value    |
| ------------------- | -------- |
| Duration            | 98.59 ms |
| Billed Duration     | 99 ms    |
| Memory Allocated    | 512 MB   |
| Maximum Memory Used | 107 MB   |

CloudWatch confirmed:

* Lambda invocation started successfully.
* All workflow stages completed successfully.
* Learning Store saved the workflow outcome.
* Workflow completed successfully.
* Lambda execution ended normally without errors.

---

# Security Review

Current security posture:

* GitHub OIDC remains configured for future CI/CD deployments.
* Lambda uses the dedicated `SisumLambdaExecutionRole`.
* No AWS access keys or secrets were committed to the repository.
* Backend currently operates using the Mock Provider.
* API authentication has not yet been implemented.

---

# Current Limitations

The following items are intentionally deferred to future sprints:

* Amazon Cognito authentication
* JWT authorization
* RBAC
* Real AWS Provider integration
* Compute Optimizer integration
* AWS Pricing API integration
* DynamoDB persistence
* Frontend integration with the live backend
* Backend GitHub Actions deployment workflow

---

# Deployment Validation Checklist

| Validation                | Status         |
| ------------------------- | -------------- |
| npm build                 | ✅ Passed       |
| Unit Tests                | ✅ 21/21 Passed |
| SAM Template Validation   | ✅ Passed       |
| SAM Build                 | ✅ Passed       |
| CloudFormation Deployment | ✅ Passed       |
| Lambda Deployment         | ✅ Passed       |
| API Gateway Deployment    | ✅ Passed       |
| Health Endpoint           | ✅ Passed       |
| Workflow Endpoint         | ✅ Passed       |
| Workflow Execution        | ✅ Passed       |
| CloudWatch Logging        | ✅ Passed       |
| Lambda Configuration      | ✅ Verified     |
| Execution Role            | ✅ Verified     |

---

# Sprint Outcome

Sprint **10.5.6** successfully delivered the first production deployment of the SISU'M backend to AWS.

The backend is now:

* Running on AWS Lambda
* Exposed through Amazon API Gateway
* Logging to Amazon CloudWatch
* Deployable using AWS SAM
* Operating in **production** using the **Mock Provider**

The platform is now ready for CI/CD automation and frontend integration.

---

# Next Sprint

## Sprint 10.5.6A – Backend CI/CD Automation

Objectives:

* Create a dedicated backend GitHub Actions workflow.
* Configure GitHub OIDC deployment using `SisumBackendDeployRole`.
* Automate validation, build, test, and deployment with AWS SAM.
* Ensure backend deployments are independent of frontend deployments.

This will be followed by:

**Sprint 10.5.7 – Frontend AWS Hosting (Amazon S3 + CloudFront + HTTPS)**
