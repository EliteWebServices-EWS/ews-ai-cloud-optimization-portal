# Sprint 10.5.7 — Frontend AWS Hosting Result

**Project:** EWS AI Cloud Optimization Platform — SISU'M
**Environment:** Production
**AWS Region:** `us-east-1`
**Status:** Completed
**Completion date:** July 15, 2026

## Objective

Migrate the EWS marketing website and SISU'M dashboard from temporary GitHub Pages hosting to Amazon CloudFront backed by a private Amazon S3 bucket.

## Deployed Resources

| Resource                   | Value                                                    |
| -------------------------- | -------------------------------------------------------- |
| CloudFormation stack       | `sisum-frontend-production`                              |
| S3 bucket                  | `sisum-frontend-production-739275446782`                 |
| CloudFront distribution ID | `E88PZIPZ6DVL4`                                          |
| CloudFront domain          | `d287wcw6yqb2r1.cloudfront.net`                          |
| CloudFront URL             | `https://d287wcw6yqb2r1.cloudfront.net`                  |
| Backend API                | `https://zqe6cl0m15.execute-api.us-east-1.amazonaws.com` |

## Implemented Infrastructure

* Private Amazon S3 frontend bucket
* S3 Block Public Access
* S3 versioning
* S3 server-side encryption
* CloudFront distribution
* CloudFront Origin Access Control
* S3 bucket policy restricted to CloudFront
* HTTPS redirect
* Compression
* SPA error handling
* CloudFront viewer-request URL rewrite function
* Directory routing from `/dashboard/` to `/dashboard/index.html`

## Frontend Build

The dashboard production build uses:

```bash
VITE_API_BASE="https://zqe6cl0m15.execute-api.us-east-1.amazonaws.com/api/v1" npm run build
```

The Vite dashboard output directory is:

```text
frontend/dashboard-dist/
```

The complete production upload package is assembled in:

```text
frontend/deploy-dist/
```

The deployment package includes:

* EWS marketing homepage
* Marketing pages and static assets
* Compiled SISU'M dashboard
* Compiled Reports page

## Production URLs

Marketing website:

```text
https://d287wcw6yqb2r1.cloudfront.net/
```

Decision Dashboard:

```text
https://d287wcw6yqb2r1.cloudfront.net/dashboard/
```

Reports:

```text
https://d287wcw6yqb2r1.cloudfront.net/dashboard/reports.html
```

## Backend Integration

The frontend production API base is:

```text
https://zqe6cl0m15.execute-api.us-east-1.amazonaws.com/api/v1
```

The backend CORS configuration now permits:

```text
https://d287wcw6yqb2r1.cloudfront.net
```

Validated endpoints:

```text
GET /api/v1/health
POST /api/v1/workflows/run
```

## Validation Results

* [x] Frontend dependency installation completed
* [x] Frontend test command completed successfully
* [x] TypeScript compilation completed
* [x] Vite production build completed
* [x] Marketing homepage loaded through CloudFront
* [x] Dashboard loaded through CloudFront
* [x] Reports navigation loaded correctly
* [x] Decision Dashboard navigation loaded correctly
* [x] Dashboard connected to the backend API
* [x] Workflow analysis completed successfully
* [x] Backend health endpoint returned HTTP 200
* [x] Workflow endpoint returned HTTP 201
* [x] CloudFront CORS origin was returned by the API
* [x] Direct public S3 access remained blocked
* [x] CloudFront directory URL rewrite worked

## Security Controls

* S3 bucket remains private
* Public S3 access is blocked
* CloudFront accesses S3 through Origin Access Control
* S3 bucket policy is restricted to the CloudFront distribution
* Frontend does not contain AWS credentials
* Frontend Vite variables contain only public configuration
* Backend continues to use the Mock Provider

## Known Limitations

* Custom domain migration is deferred to a later sprint
* Frontend GitHub Actions deployment is not yet implemented
* Cognito authentication is not yet implemented
* The production API remains unauthenticated
* Real AWS Provider integration is not yet enabled
* Generated frontend files are still uploaded manually

## Completion Decision

Sprint 10.5.7 is complete.

The EWS marketing website and SISU'M dashboard are now served securely through Amazon CloudFront using a private S3 origin. The dashboard successfully communicates with the deployed API Gateway and Lambda backend.
